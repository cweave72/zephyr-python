[project]
name = "trace-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.1.3",
    "rich>=13.9.4",
]

[project.scripts]
trace-tool = "trace_tool.cli:entrypoint"
